package main

import "fmt"

func usage() {
	fmt.Println(`runeflow usage

runeflow register - create a new Runeflow account
runeflow auth     - add this server to your Runeflow account

For more details, check the manual page for runeflow(1).
`)
}
